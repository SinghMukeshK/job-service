version: 0.2
phases:
  install:
    runtime-versions:
        nodejs: 10
    commands:
      - npm install -g serverless
      - cd serverless
      - ls
      - npm install
      - serverless deploy --stage=dev
  # build:
  #   commands: