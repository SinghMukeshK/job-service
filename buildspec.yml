version: 0.2
phases:
  install:
    runtime-versions:
        nodejs: 10
    commands:
      - npm install
      - npm install  serverless
      - ls
      - cd serverless
      - ls
      - serverless deploy --stage=dev
  # build:
  #   commands: